<view class="page tw-page"><view class="eyebrow">HELP / GUIDE</view><view class="title">使用指南</view><view class="subtitle">按“建班 → 建档 → 日常记录 → 定期备份”的顺序使用教师工作台。</view><view class="section-title">第一次使用</view><view class="steps"><view wx:for="{{steps}}" wx:key="no" class="step"><text class="step-no">{{item.no}}</text><view><text class="step-title">{{item.title}}</text><text class="step-text">{{item.text}}</text></view></view></view><view class="section-title">模块说明</view><view class="module-grid"><view wx:for="{{modules}}" wx:key="title" class="module"><text class="module-title">{{item.title}}</text><text class="module-text">{{item.text}}</text></view></view><view class="notice">数据导入遵循永久兼容规则；文件附件与结构化数据分开处理，小程序不会自动上传客户端附件。</view></view>
