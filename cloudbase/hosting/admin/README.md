# 管理员后台托管

后台只允许唯一管理员微信身份访问。部署前必须在 `admin` 云函数配置 `ADMIN_OPENID` 环境变量，不能把管理员 OpenID 写入前端。

部署顺序：

1. 将 `admin/` 构建或托管到 CloudBase 静态托管；
2. 配置 CloudBase Web 登录并取得当前微信身份；
3. 通过 `admin` 云函数执行生成、撤销兑换码和查看授权记录；
4. 后台前端不保存兑换码明文，不直接写数据库。

当前仓库只提供安全边界和后台页面骨架，未写入真实管理员身份或兑换码。
