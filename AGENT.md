# 当前工作目录协作规范

## 版本与文档

- 当前产品版本以 `package.json` 为主来源，并必须通过 `npm run version:check`。
- 新版本规划文档必须在文件名和标题中明确日期、目标版本、主题和文档类型。
- 推荐格式：`YYYY-MM-DD-vX.Y.Z-主题-文档类型.md`。
- 文档类型使用明确词汇：`规划`、`设计说明`、`实施计划`、`审计报告`、`发布检查清单`、`版本总结`。
- 不再新建“下一阶段功能规划”“后续优化方案”“专项问题分析与优化方案”等无法表达版本和用途的模糊名称。
- 规划文档必须反映代码真实状态，写明基线、目标、非目标、范围、接口/数据影响、验收标准和风险。
- 版本发布后，必须同步更新 README、对应版本总结和发布检查清单；规划文档须标记为“已发布”或“已归档”，不得继续保留“规划中”。
- 发布检查清单只能勾选有命令输出、Release 资产摘要或人工记录支撑的项目；未完成的真实数据迁移、GUI 人工验收和便携包 QA 必须保留为明确待办，并记录失败环节。

## 修改与验证

- 修改版本或规划文档后，用 `rg` 检查旧版本号、旧文件名和失效引用。
- 新功能必须遵循测试先行：先写失败测试，再写最小实现，再运行完整测试。
- 涉及数据库迁移、备份、上传、桌面运行时或发布脚本时，必须补充对应集成测试和发布验证。
- 不在 `master` 直接实现功能；功能开发使用 `.worktrees/` 下的 `codex/` 分支。
