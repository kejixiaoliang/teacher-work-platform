# 当前工作目录协作规范

- 当前产品版本以 `package.json` 为主来源，并必须通过 `npm run version:check`。
- 新版本文档使用 `YYYY-MM-DD-vX.Y.Z-主题-文档类型.md`，文档类型使用 `规划`、`设计说明`、`实施计划`、`审计报告`、`发布检查清单`、`版本总结`。
- 不再新建“下一阶段功能规划”“后续优化方案”等无法表达版本和用途的模糊名称。
- 规划文档必须反映代码真实状态，写明基线、目标、非目标、范围、验收标准和风险。
- 新功能遵循测试先行；涉及数据库、备份和发布链路时必须补充集成测试。
- 功能开发使用 `.worktrees/` 下的 `codex/` 分支，不在 `master` 直接实现。
