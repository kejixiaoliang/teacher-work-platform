# 教师工作台 v0.9.0 Windows 发布检查清单

本清单用于 v0.9.0 安装版、便携版和自动更新发布。没有完成的项目不得把 `latest.json` 指向生产用户。

## 已在当前分支完成

- [x] package、Cargo、Tauri 配置和前端版本统一为 `0.9.0`。
- [x] 安装版使用 current-user NSIS，数据目录为 `%LOCALAPPDATA%\TeacherWork\data`。
- [x] 便携版保持 EXE 同级 `data`，两种交付形式共用数据库迁移和备份格式。
- [x] Tauri updater 只在安装版启用，便携版继续手动升级。
- [x] 更新前阻止未保存座位变更，创建带附件的 `pre-update-*` 恢复点。
- [x] 安装包、`.sig`、`latest.json` 和 SHA-256 清单有生成与校验脚本。
- [x] 本地 fixture 覆盖无更新、发现更新、签名错误、资源不存在和断网。
- [x] 全量 Node 测试、Vite 构建、便携构建和安装版测试密钥构建通过。
- [x] v0.8 风格旧库升级到数据库版本 8 后，记录、附件和迁移恢复点仍存在。
- [x] 本机安装验收确认安装版内置 `resources\runtime\node.exe` 和 `resources\app\server\index.js`，并完成数据库首次创建与迁移。
- [x] 定位并修复安装版使用 HTTP updater endpoint 导致 Tauri setup 阶段闪退的问题；构建入口现在拒绝非 HTTPS endpoint。
- [x] 使用真实安装版恢复 v0.8 导出的完整备份，恢复后工作台可以继续使用。
- [x] CloudBase 静态托管测试通过：测试安装包、`.sig`、`checksums.json` 和 `latest.json` 已上传到隔离测试目录；未发布到 `stable/latest.json`。
- [x] 已在仓库之外的本机专用目录生成生产 updater 密钥，并使用生产公钥构建 CloudBase 测试安装包；私钥未上传或提交。
- [x] 定位并修复真实升级演练中的 Vue Proxy 错误；更新结果改用 `shallowRef` 保存，包含修复的 v0.9.0 安装包已重新构建。
- [x] 定位并修复 Windows 更新器重复 relaunch 导致的 `node.exe` / `better_sqlite3.node` 文件占用问题。
- [x] 更新安装前显式停止并等待本地 Node 后台服务，避免 `better_sqlite3.node` 退出竞态；新的 0.9.1 测试包已重新上传 CloudBase 测试目录。
- [x] 增加 NSIS 安装前 hook：关闭当前应用树，并只清理当前安装目录对应的残留 `node.exe`。
- [x] 增加安装版单实例互斥、Windows Job Object 后台进程托管和异常退出后的精确残留清理。
- [x] 全量 Node 测试 134/134 通过，Rust 桌面层测试 5/5 通过。
- [x] 已生成新的 0.9.0 进程保护最终验收包：`E:\工作台测试\教师工作台_0.9.0_进程保护最终验收-setup.exe` 及对应 `.sig`。
- [x] 修正多配置构建时 NSIS hook 未显式注入和 PowerShell 嵌套引号问题，并重新生成二次修复验收包：`E:\工作台测试\教师工作台_0.9.0_进程保护二次修复-setup.exe` 及对应 `.sig`。
- [x] 修正 NSIS 对 PowerShell `$` 变量的转义遗漏，并重新生成三次修复验收包：`E:\工作台测试\教师工作台_0.9.0_进程保护三次修复-setup.exe` 及对应 `.sig`；待模拟残留进程后进行最终验收。

## 发布电脑上必须补做

- [ ] 使用生产签名私钥构建，不把私钥写入仓库、日志或发布资产。
- [ ] 完成 Windows Authenticode 代码签名，并记录证书指纹和签名时间。
- [ ] 在干净 Windows 10/11 x64 虚拟机执行安装、首次启动、重启和卸载。
- [ ] 通过真实用户双击安装器完成一次 GUI 首次启动、关闭和再次启动验收；自动化启动只作为辅助证据。
- [ ] 验证安装路径包含空格、中文用户目录、普通用户权限和重复安装覆盖。
- [ ] 验证卸载不删除 `%LOCALAPPDATA%\TeacherWork`，重新安装可读回旧数据。
- [ ] 用真实 v0.8.0 安装版和便携版数据分别执行完整备份互导。
- [ ] 用生产公钥构建后验证无更新、可更新、下载失败、签名错误和断网行为。
- [ ] 使用包含 Proxy 修复的 v0.9.0 客户端完成从 v0.9.0 到 v0.9.1 的真实下载、验签、安装、重启和数据保留验收。
- [ ] 在应用保持打开、后台服务正常运行和异常退出残留三种状态下执行覆盖安装，确认不再出现 `better_sqlite3.node` / `runtime\\node.exe` 写入错误；出现错误时不得选择“忽略”继续发布验收。
- [ ] 连续双击安装版启动图标，确认只有一个实例和一个本安装目录的 Node 后台进程。
- [ ] 上传不可变版本目录中的安装包和 `.sig`，从公网重新下载并复核 SHA-256。
- [ ] 最后上传 `stable/latest.json`，确认清单中的 URL、签名、版本和 notes 均正确。

## 发布后回滚规则

1. 先停止发布 `latest.json` 的更新。
2. 保留用户数据目录和 `pre-update-*` 恢复点，不删除或覆盖原始备份。
3. 回退时同时使用匹配版本的程序和数据库/完整备份，不只替换 EXE。
4. 修复发布问题时递增版本号和不可变资产路径，不原地替换已发布安装包。
