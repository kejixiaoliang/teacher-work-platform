# v0.5.0 班主任日常工作台发布检查清单

> 归档说明：历史版本资料，仅用于追溯当时的实现与发布过程。

> 目标版本：v0.5.0  
> 基线版本：v0.4.2  
> 发布标签：`v0.5.0`
> 发布提交：`499df81582cdb24d108a1d624d6127ef5b152d6b`
> GitHub Release：[v0.5.0](https://github.com/kejixiaoliang/teacher-work-platform/releases/tag/v0.5.0)

## 已完成

- [x] 数据库升级到 user_version 5，新增跟进事项表和索引。
- [x] 跟进事项新增、查询、编辑、完成、取消和重新打开接口通过测试。
- [x] 跟进事项拒绝无效日期、跨班级学生和无效状态。
- [x] 全量备份和班级备份包含 `follow_up_tasks`。
- [x] 今日工作台接口聚合考勤、请假、值日、近期考试和跟进事项。
- [x] 学生详情支持跟进事项维护，概览首页支持工作台跳转。
- [x] `npm test`：60/60 通过。
- [x] `npm run build`：通过。
- [x] `npm run version:check`：通过，产品版本为 0.5.0。
- [x] `npm audit --omit=dev --audit-level=moderate`：0 vulnerabilities。
- [x] `npm run tauri:build`：通过，生成 release EXE。
- [x] `npm run package:portable`：通过，生成 v0.5.0 Windows x64 ZIP。
- [x] 便携包已上传 GitHub Release，文件大小 46,653,254 字节。
- [x] 便携包 SHA-256：`BDED8A2A282E638791DBD136D57BE53A1EAA927042C8BA160D1E0F9879C6006B`，与 GitHub Release 资产摘要一致。
- [x] `git diff --check`：通过。

## 发布后仍需补充的验证

- [ ] 使用一份真实但脱敏的 v0.4.2 数据执行迁移，并核对迁移前快照可恢复；该项属于数据升级人工验收，未因 Release 发布而虚假标记完成。
- [ ] 在浏览器和 Tauri WebView 中人工检查首页工作台、学生详情跟进事项和日期显示。
- [ ] 便携包 QA：已验证 ZIP 可解压、EXE 可启动并创建数据库；但自动化脚本在请求正常关闭后，主进程未在 10 秒内退出，需在干净 Windows 环境定位并复测。
- [ ] 检查 ZIP 恢复后跟进事项仍存在且能正常编辑。

## 已知范围

学生成长报告、班级月报、本地临时事项和系统级提醒暂不在本批实现，列入后续版本规划。
