# v0.4.2 发布检查清单

> 归档说明：历史版本资料，仅用于追溯当时的实现与发布过程。

## 已通过

- [x] ZIP 归档生成、解压和 SHA-256 校验测试通过。
- [x] ZIP 路径穿越、重复条目、文件数量和总大小限制已覆盖。
- [x] 数据库与上传附件一并导出、删除后恢复测试通过。
- [x] 旧版 JSON 备份导入兼容保留。
- [x] 文件 token 30 秒过期、绑定文档、成功消费即失效。
- [x] 一次性文件 token 不能访问普通 API。
- [x] 前端文档预览/下载改用 Blob URL，不再拼接长期 API token。
- [x] 自动化测试 50/50 通过。
- [x] `npm audit --omit=dev --audit-level=moderate`：0 vulnerabilities。

## 待执行

- [x] `npm run build`
- [x] VS Build Tools x64 环境运行 `npm run tauri:build`
- [x] `npm run package:portable`
- [x] `npm run qa:portable -- -ZipPath release\\教师工作台-v0.4.2-windows-x64-portable.zip`
- [x] 记录最终 ZIP SHA-256：`d037f15c1899b8943c01aa1b62b58462b5240362e0c14053564cd8c3088a4280`

## 后续观察项

- 目前一次性授权 token 存储在服务进程内存，服务重启后自然失效；多进程部署时需要共享存储。
- ZIP 恢复当前按附件文件名覆盖 `data/files`，后续可增加更细粒度的冲突预览和恢复报告。
