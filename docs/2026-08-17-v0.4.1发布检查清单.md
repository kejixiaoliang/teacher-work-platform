# v0.4.1 发布检查清单

## 已通过

- [x] 版本契约脚本可执行，当前代码仍保持 `0.4.0`，未提前改版本号。
- [x] Node 自动化测试 46/46 通过。
- [x] Vite 生产构建通过。
- [x] 核心 API 错误响应统一为 4xx/稳定错误码。
- [x] 备份重复主键预检、恢复前快照、事务和外键检查通过。
- [x] 上传 BMP、WebP、旧 Office 文件签名校验通过；伪造 WebP 被拒绝。
- [x] 文件响应增加 `nosniff`、`no-referrer` 和 `no-store`。
- [x] 学生和值日批量删除能展示成功/失败数量。
- [x] 表现量化记录/统计加载增加过期响应保护。

## 阻断或待确认

- [ ] `npm audit --omit=dev --audit-level=moderate`：9 个 high，路径主要为 `nanoid -> postcss -> Vue/Element Plus`，当前报告无可用修复版本。需要登记风险豁免，或在有兼容升级方案后重新验证。
- [ ] Tauri Release EXE：当前 worktree 缺少 `src-tauri/target/release/teacher-work.exe`，无法执行真实便携包组装和 QA。
- [ ] Windows 便携包：需要在可用 MSVC/Rust/Tauri 环境运行 `npm run tauri:build`、`npm run package:portable` 和 `npm run qa:portable`。
- [ ] 大数据量分页：当前量化流水、文档和请假列表仍需在真实大数据集上压测；若超过可接受阈值，应进入后续分页版本，不阻塞已验证的错误契约修复。
- [ ] 文件 URL query token：当前已降低 Referer/缓存暴露，但尚未改为一次性授权 URL；如需更高安全等级，应单独设计文件授权接口。

## 发布判断

在依赖风险豁免和便携包 QA 未完成前，不建议打 `v0.4.1` 正式发布标签。代码层面可以作为 v0.4.1 候选提交，待环境门禁完成后再同步版本号、变更记录和 Release 资产。
