# v0.5.0 班主任日常工作台发布检查清单

> 目标版本：v0.5.0  
> 基线版本：v0.4.2  
> 当前分支：`codex/v0.5.0-workbench`

## 已完成

- [x] 数据库升级到 user_version 5，新增跟进事项表和索引。
- [x] 跟进事项新增、查询、编辑、完成、取消和重新打开接口通过测试。
- [x] 跟进事项拒绝无效日期、跨班级学生和无效状态。
- [x] 全量备份和班级备份包含 `follow_up_tasks`。
- [x] 今日工作台接口聚合考勤、请假、值日、近期考试和跟进事项。
- [x] 学生详情支持跟进事项维护，概览首页支持工作台跳转。
- [x] `npm test`：57/57 通过。
- [x] `npm run build`：通过。
- [x] `npm run version:check`：通过，产品版本为 0.5.0。
- [x] `npm audit --omit=dev --audit-level=moderate`：0 vulnerabilities。
- [x] `npm run tauri:build`：通过，生成 release EXE。
- [x] `npm run package:portable`：通过，生成 v0.5.0 Windows x64 ZIP。
- [x] 便携包 SHA-256：`8112F8CF217BDCB1FF4B7AC89A96EC526BAD9657DB3208F50F65081F380A1BDC`。
- [x] `git diff --check`：通过。

## 发布前待执行

- [ ] 使用真实 v0.4.2 数据执行一次迁移和回滚前快照检查。
- [ ] 在浏览器和 Tauri WebView 中人工检查首页工作台、学生详情跟进事项和日期显示。
- [ ] 便携包 QA：当前因 QA 脚本遗留进程持续占用 ZIP，无法完成本轮解压校验；需在干净环境复测。
- [ ] 检查 ZIP 恢复后跟进事项仍存在且能正常编辑。

## 已知范围

学生成长报告、班级月报、本地临时事项和系统级提醒暂不在本批实现，列入 V0.5.0 后续批次。
